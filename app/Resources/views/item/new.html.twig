{% extends 'base.html.twig' %}

{% block body %}
    <h1>Item creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-light">Create</button>
    {{ form_end(form) }}

            <a href="{{ path('item_index') }}"><button type="button" class="btn btn-light">Back to the list</button></a>
{% endblock %}
